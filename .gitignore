# Rust
/target/
**/*.rs.bk
*.pdb

# Bun / node
node_modules/
web/dist/
.bun/
*.tsbuildinfo
npm-debug.log*
bun-debug.log*

# Anything this machine holds and the repo does not: pulled artwork, notes on
# a review, whatever a session leaves behind. One place, so it does not have
# to be added to a line at a time.
/local/

# Local data — the DB is a disposable index, never committed
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
/data/
scryfall-bulk/
# Exported from the cache by the binary, then uploaded
/catalog/

# Secrets / local config
.env
.env.*
!.env.example
*.pem
*.key

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
.directory
.Trash-*
.nfs*

# Claude Code: shared settings are committed, personal overrides are not
.claude/settings.local.json

# Editors
.idea/
*.swp
*.swo
.vscode/*
!.vscode/extensions.json
